version: '3.8'

services:
  # PostgreSQL 数据库
  postgres:
    image: postgres:16-alpine
    container_name: share-platform-db
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: share_platform
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme123}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis 缓存
  redis:
    image: redis:7-alpine
    container_name: share-platform-redis
    restart: unless-stopped
    ports:
      - "6380:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # NestJS 后端
  backend:
    build:
      context: ./packages/backend
      dockerfile: Dockerfile
    container_name: share-platform-backend
    restart: unless-stopped
    network_mode: "host"
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://admin:${POSTGRES_PASSWORD:-changeme123}@localhost:5433/share_platform
      REDIS_HOST: localhost
      REDIS_PORT: 6380
      ACCESS_PASSWORD: ${ACCESS_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      MAX_FILE_SIZE: ${MAX_FILE_SIZE:-10485760}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./uploads:/app/uploads

  # Next.js 前端
  frontend:
    build:
      context: ./packages/frontend
      dockerfile: Dockerfile
    container_name: share-platform-frontend
    restart: unless-stopped
    network_mode: "host"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_WS_URL: ${NEXT_PUBLIC_WS_URL:-http://localhost:3001}
    depends_on:
      - backend

volumes:
  postgres-data:
    driver: local
  redis-data:
    driver: local
