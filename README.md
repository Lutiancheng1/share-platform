# Share Platform (跨设备共享平台)

一个现代化的跨设备文件和文本共享平台，支持实时通信、文件拖拽上传、图片预览等功能。

## ✨ 功能特性

- **实时通信**: 基于 Socket.IO 的实时消息同步
- **文件共享**: 支持拖拽上传文件和图片
- **图片预览**: 集成 `react-photo-view`，支持缩放、平移和旋转
- **多端适配**: 响应式设计，完美适配移动端和桌面端
- **局域网访问**: 自动获取局域网 IP，方便同一网络下的设备互联
- **访客机制**: 支持生成邀请链接，访客免登录即可加入
- **Markdown**: 完善的文档和代码展示支持

## 🛠 技术栈

### Frontend (前端)
- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **UI Library**: Tailwind CSS, Radix UI
- **State/Icons**: Lucide React
- **Image Preview**: react-photo-view

### Backend (后端)
- **Framework**: NestJS
- **Language**: TypeScript
- **Real-time**: Socket.IO
- **File Handling**: Multer

## 🚀 快速开始

### 1. 环境要求
- Node.js >= 18
- npm or yarn or pnpm

### 2. 安装依赖

在项目根目录下运行：

```bash
./start.sh
```

或者手动安装：

```bash
# 安装前端依赖
cd packages/frontend
npm install

# 安装后端依赖
cd ../backend
npm install
```

### 3. 启动项目

我们提供了一个便捷的启动脚本，可以同时启动前端和后端服务：

```bash
# 在项目根目录运行
./start.sh
```

启动后：
- **前端地址**: `http://localhost:3000` (或局域网 IP:3000)
- **后端地址**: `http://localhost:3001` (或局域网 IP:3001)

### 4. 常用命令

#### 前端
```bash
cd packages/frontend
npm run dev   # 开发模式
npm run build # 构建生产版本
npm start     # 运行生产版本
```

#### 后端
```bash
cd packages/backend
npm run start:dev # 开发模式
npm run build     # 构建
npm run start:prod # 运行生产版本
```

## 📂 项目结构

```
share-platform/
├── packages/
│   ├── frontend/          # Next.js 前端项目
│   │   ├── app/           # 页面路由
│   │   ├── components/    # React 组件
│   │   └── lib/           # 工具函数和类型定义
│   └── backend/           # NestJS 后端项目
│       ├── src/
│       │   ├── modules/   # 业务模块 (upload, invite, etc.)
│       │   └── ...
└── start.sh               # 一键启动脚本
```

## 📝 注意事项

- 确保 3000 和 3001 端口未被占用。
- 局域网访问时，请确保防火墙允许通过这两个端口。
- 上传文件大小限制默认为 50MB (可在后端配置中修改)。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 License

MIT
